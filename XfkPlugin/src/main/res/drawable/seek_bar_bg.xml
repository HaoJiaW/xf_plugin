<?xml version="1.0" encoding="utf-8"?>
<layer-list
    xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:id="@android:id/background"  android:gravity="center" android:height="2dp">
        <shape>
            <solid android:color="@color/white" />
        </shape>
    </item>
    <item android:id="@android:id/progress" android:gravity="center" android:height="2dp">
        <clip>
            <shape>
                <solid android:color="@color/yellow" />
            </shape>
        </clip>
    </item>
</layer-list>